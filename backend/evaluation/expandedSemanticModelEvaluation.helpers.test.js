const { canonicalTopics, reconstruct, crossValidate } = require('./expandedSemanticModelEvaluation.helpers');
const { formatStructuredContext } = require('./sbertInputRepresentation.helpers');
test('deduplicates canonical strings and reconstructs pairs',()=>{const cases=[{id:'a',expected_class:'LOW',category:'x',submitted:{title:'A'},existing:{title:'B'}},{id:'b',expected_class:'HIGH',category:'x',submitted:{title:'A'},existing:{title:'A'}}], inputs=canonicalTopics(cases,formatStructuredContext), vectors=new Map(inputs.map((x,i)=>[x,i?[0,1]:[1,0]]));expect(inputs).toHaveLength(2);expect(reconstruct(cases,formatStructuredContext,vectors,'x')).toHaveLength(2);});
test('uses stratified held-out folds without training leakage',()=>{const results=['LOW','MEDIUM','HIGH'].flatMap(c=>Array.from({length:5},(_,i)=>({id:c+i,expected_class:c,scores:{x:{LOW:.1,MEDIUM:.5,HIGH:.9}[c]+i/100}})));const cv=crossValidate(results,'x',7);expect(cv.folds).toHaveLength(5);expect(cv.folds.every(x=>x.heldOutCases===3&&x.trainingCases===12&&x.t1<x.t2)).toBe(true);});
